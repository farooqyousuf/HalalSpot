<% if user_signed_in? %>

<!-- For Google Maps -->
<% #<%= gmaps4rails(@json) %>

<h1>Listing Halal Spots</h1>

<%= form_tag places_path, :method => :get do %>
<p>
	<%= text_field_tag :search, params[:search] %>
	<%= submit_tag "Search Nearby Halal Spots", :name => nil %>
</p>
<% end %>

<table class="table table-striped">
	<thead>
  		<tr>			
			 <th>Image</th>
		   <th>Name</th>
		   <th>Address</th>
			 <th>State</th>
			 <th>City</th>
			 <th>Type</th>
		   <th>Description</th>
		   <th>Website</th>
		   <th></th>
		   <th></th>
		   <th></th>
		 </tr>
	</thead>

	<tbody>
		<%= render @places %>
	</tbody>

</table>

<br />

<% else %>

<%= render 'static_pages/home' %>

<% end %>