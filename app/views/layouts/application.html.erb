<!DOCTYPE html>
<html>
<head>
  <title>Halal Spot</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
  <%= yield :head %>
</head>
<body>
	
	<%= render 'layouts/header' %>	
	
	<div class="container">

			<% flash.each do |name, msg| %>
				<%= content_tag :div, msg, class: "alert alert-#{name}" unless msg.blank? %>
				<% end %>
		
		<%= yield %>
		<!-- For Google Maps -->
		<!-- <%= yield :scripts %> -->
		
		</div>
		
	<%= render 'layouts/footer' %>

</body>
</html>